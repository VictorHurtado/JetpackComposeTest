

import org.junit.Test
import org.junit.Assert.assertEquals

class CalculationUnitTest {
/*
    @Test
    fun calculatePrice_Correct() {
        val price = 100.0
        val discount = 10.0
        val expected = 90.0

        val result = calculatePrice(price, discount)
        assertEquals(expected, result, 0.0) // Verifica si el resultado es igual al esperado con un margen de 0.0
    }

    @Test
    fun calculateDiscount_Correct() {
        val price = 100.0
        val discount = 10.0
        val expected = 10.0

        val result = calculateDiscount(price, discount)
        assertEquals(expected, result, 0.0) // Verifica si el resultado es igual al esperado con un margen de 0.0
    }
    */
}
